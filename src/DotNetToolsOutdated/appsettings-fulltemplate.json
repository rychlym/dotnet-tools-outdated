{
  // Here are settings, that can be set for the http client used in the application.
  // The settings correspond to the properties of the HttpClientHandler class.
  // (For more details see https://learn.microsoft.com/en-us/dotnet/api/system.net.http.httpclienthandler )

  // Please do not edit here as this is for the documentation purpose only; instead edit the appsettings.json .
  // The settings specified here have are the default values. That means if you do not specify any settings in the appsettings.json,
  // these values will be used by the application.
  // If you want to override any of these settings, please move the needed row in the appsettings.json file with an opposite boolean value or a different one.
  "httpClient": {
    "allowAutoRedirect": true,
    "checkCertificateRevocationList": true,
    "preAuthenticate": false,
    "useDefaultCredentials": false,
    "useCookies": true,
    "useProxy": true,
    // this sets Proxy property of HttpClientHandler to the WebRequest.DefaultWebProxy
    // (it seems to be good to leave useProxy true and set preAuthenticate to true in this case)
    "useDefaultWebProxy": false
  }
}